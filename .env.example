# Copy to ~/.pi/agent/.env (never commit real keys — .env is gitignored).
# Read by extensions via process.env first, then this file.

# Exa API key for web_search (https://exa.ai — 20k requests/month free).
EXA_API_KEY=
